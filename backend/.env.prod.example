# Production database URL (used only for running seed from your machine)
# Copy this file to .env.prod and paste your Railway PUBLIC Postgres URL.
# Get it from: Railway → your project → PostgreSQL service → Connect → "Public network" URL.
#
# DATABASE_URL=postgresql://postgres:YOUR_PASSWORD@YOUR_PROJECT.proxy.rlwy.net:PORT/railway

DATABASE_URL=
